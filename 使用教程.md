# Symphony 使用教程

Symphony 将项目工作转化为隔离的、自主的实现任务，让团队管理「工作」而非监督编码 Agent。

---

## 目录

- [环境准备](#环境准备)
- [安装步骤](#安装步骤)
- [配置说明](#配置说明)
- [启动运行](#启动运行)
- [Web 仪表盘](#web-仪表盘)
- [测试](#测试)
- [Claude Code 模式 vs Codex 模式](#claude-code-模式-vs-codex-模式)
- [常见问题](#常见问题)

---

## 环境准备

### 1. Elixir / Erlang 环境

推荐使用 [mise](https://mise.jdx.dev/) 管理 Elixir 和 Erlang 版本：

```bash
# 安装 mise（如尚未安装）
curl https://mise.run | sh

# 进入项目目录
cd symphony-main/elixir
mise trust
mise install

# 验证安装
mise exec -- elixir --version
```

### 2. Linear API Token

Symphony 通过 Linear 轮询待办事项，需要个人 API 令牌：

1. 打开 Linear → Settings → Security & access → Personal API keys
2. 创建新的令牌并复制

```bash
export LINEAR_API_KEY="你的令牌"
```

### 3. Agent 工具

根据选择的 Agent 模式，需要安装对应的工具：

**Codex 模式（默认）：**
- 安装 OpenAI Codex CLI 并配置 API Key

**Claude Code 模式：**
- 安装 Claude Code CLI：参照 Anthropic 官方文档
- 完成认证：`claude auth`
- 确保 `ANTHROPIC_API_KEY` 环境变量已设置，或拥有有效的 Claude 订阅

### 4. Linear 自定义状态

Symphony 依赖以下非默认的 Linear issue 状态，需在 Team Settings → Workflow 中手动创建：

| 状态名 | 用途 |
|--------|------|
| Rework | 审查后需要返工 |
| Human Review | 等待人工审查 |
| Merging | 已批准，执行合并流程 |

---

## 安装步骤

```bash
cd symphony-main/elixir

# 安装依赖
mise exec -- mix setup

# 构建可执行文件
mise exec -- mix build
```

构建完成后会在 `bin/symphony` 生成可执行文件。

---

## 配置说明

Symphony 的核心配置文件是 `WORKFLOW.md`，采用 YAML front matter + Markdown prompt 的格式。

### 完整配置示例

```md
---
tracker:
  kind: linear
  project_slug: "your-project-slug"
  active_states:
    - Todo
    - In Progress
    - Merging
    - Rework
  terminal_states:
    - Closed
    - Cancelled
    - Canceled
    - Duplicate
    - Done
polling:
  interval_ms: 5000
workspace:
  root: ~/code/symphony-workspaces
hooks:
  after_create: |
    git clone --depth 1 https://github.com/your-org/your-repo .
    if command -v mise >/dev/null 2>&1; then
      cd elixir && mise trust && mise exec -- mix deps.get
    fi
  before_remove: |
    cd elixir && mise exec -- mix workspace.before_remove
agent:
  kind: claude_code
  max_concurrent_agents: 10
  max_turns: 20
codex:
  command: codex app-server
  approval_policy: never
  thread_sandbox: workspace-write
  turn_sandbox_policy:
    type: workspaceWrite
claude_code:
  command: claude
  model: sonnet
  permission_mode: bypassPermissions
  turn_timeout_ms: 600000
  read_timeout_ms: 300000
---

在此处编写 Agent 的 prompt 模板...
```

### 配置项详解

#### tracker（追踪器）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `kind` | `linear` | 追踪器类型，目前仅支持 `linear` |
| `project_slug` | 必填 | Linear 项目 slug，可从项目 URL 中获取 |
| `api_key` | 环境变量 | 建议使用 `$LINEAR_API_KEY` 引用环境变量 |
| `active_states` | `["Todo", "In Progress"]` | 触发 Agent 执行的状态列表 |
| `terminal_states` | 见默认值 | 标记任务完成的状态列表 |

#### polling（轮询）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `interval_ms` | `30000` | 轮询 Linear 的间隔（毫秒） |

#### workspace（工作区）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `root` | 系统临时目录 | 工作区根目录，支持 `~` 和 `$VAR` 展开 |

#### hooks（钩子）

| 字段 | 说明 |
|------|------|
| `after_create` | 工作区创建后执行，通常用于 clone 仓库 |
| `before_run` | Agent 运行前执行 |
| `after_run` | Agent 运行后执行 |
| `before_remove` | 工作区删除前执行 |
| `timeout_ms` | 钩子超时时间，默认 `60000` |

#### agent（Agent 配置）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `kind` | `codex` | Agent 类型：`codex` 或 `claude_code` |
| `max_concurrent_agents` | `10` | 最大并发 Agent 数 |
| `max_turns` | `20` | 单次调用的最大连续轮次 |
| `max_retry_backoff_ms` | `300000` | 重试退避最大时间（毫秒） |

#### codex（Codex 配置）

仅在 `agent.kind: codex` 时生效。

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `command` | `codex app-server` | Codex CLI 命令 |
| `approval_policy` | 见说明 | 审批策略 |
| `thread_sandbox` | `workspace-write` | 沙箱模式 |
| `turn_timeout_ms` | `3600000` | 单轮超时（毫秒） |
| `read_timeout_ms` | `5000` | 读取超时（毫秒） |
| `stall_timeout_ms` | `300000` | 停滞超时（毫秒） |

#### claude_code（Claude Code 配置）

仅在 `agent.kind: claude_code` 时生效。

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `command` | `claude` | Claude Code CLI 命令路径 |
| `model` | `null`（使用默认） | 模型选择，如 `sonnet`、`opus` |
| `permission_mode` | `bypassPermissions` | 权限模式 |
| `turn_timeout_ms` | `600000` (10分钟) | 单轮超时（毫秒） |
| `read_timeout_ms` | `300000` (5分钟) | 读取超时（毫秒） |

---

## 启动运行

### 基本启动

```bash
cd symphony-main/elixir

# 设置环境变量
export LINEAR_API_KEY="你的令牌"

# 启动（使用默认 WORKFLOW.md）
mise exec -- ./bin/symphony ./WORKFLOW.md
```

### 使用自定义配置文件

```bash
mise exec -- ./bin/symphony /path/to/custom/WORKFLOW.md
```

### 可选启动参数

| 参数 | 说明 |
|------|------|
| `--logs-root <路径>` | 日志输出目录，默认 `./log` |
| `--port <端口号>` | 启用 Web 仪表盘和 JSON API |

示例：

```bash
# 启动并开启仪表盘
mise exec -- ./bin/symphony ./WORKFLOW.md --port 4000

# 自定义日志目录
mise exec -- ./bin/symphony ./WORKFLOW.md --logs-root /var/log/symphony
```

### 运行流程

启动后 Symphony 会自动执行以下循环：

1. **轮询 Linear** — 按 `polling.interval_ms` 间隔检查新任务
2. **认领任务** — 将符合条件的 issue（如 Todo 状态）标记为 In Progress
3. **创建工作区** — 在 `workspace.root` 下为每个 issue 创建隔离目录
4. **执行钩子** — 运行 `after_create` 初始化工作区（如 clone 仓库）
5. **启动 Agent** — 根据 `agent.kind` 调用 Codex 或 Claude Code
6. **持续工作** — Agent 自主执行任务，直到完成或达到 `max_turns`
7. **状态流转** — Agent 自动管理 issue 状态（In Progress → Human Review → Merging → Done）
8. **清理工作区** — 任务完成后运行 `before_remove` 并删除工作区

---

## Web 仪表盘

使用 `--port` 参数启动后，可通过浏览器访问：

| 路径 | 说明 |
|------|------|
| `/` | LiveView 实时仪表盘 |
| `/api/v1/state` | 全局运行状态 JSON |
| `/api/v1/<issue_identifier>` | 单个 issue 状态 JSON |
| `/api/v1/refresh` | 触发状态刷新 |

---

## 测试

```bash
cd symphony-main/elixir

# 运行单元测试
make test

# 运行测试并查看覆盖率
make coverage

# 运行完整 CI 流程（格式检查 + lint + 测试 + 覆盖率 + 类型分析）
make all
```

### 端到端测试

需要真实的 Linear API Key 和 Agent 环境：

```bash
export LINEAR_API_KEY="你的令牌"
make e2e
```

可选环境变量：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SYMPHONY_LIVE_LINEAR_TEAM_KEY` | `SYME2E` | Linear 团队标识 |
| `SYMPHONY_LIVE_SSH_WORKER_HOSTS` | Docker 容器 | SSH Worker 主机列表（逗号分隔） |

---

## Claude Code 模式 vs Codex 模式

| 对比项 | Codex 模式 | Claude Code 模式 |
|--------|-----------|-----------------|
| `agent.kind` | `codex`（默认） | `claude_code` |
| Agent 工具 | OpenAI Codex CLI | Anthropic Claude Code CLI |
| 认证方式 | OpenAI API Key | `ANTHROPIC_API_KEY` 或 Claude 订阅 |
| 权限控制 | Codex sandbox | `--dangerously-skip-permissions` |
| 动态工具 | 支持 `linear_graphql` | 暂不支持（未来通过 MCP 集成） |

切换方式：修改 `WORKFLOW.md` 中的 `agent.kind` 字段即可，无需修改其他模块。

---

## 常见问题

### Q: 启动时报 WORKFLOW.md 配置错误？

检查 YAML front matter 格式是否正确。如果 reload 失败，Symphony 会使用上次有效的配置继续运行，并持续报错直到文件修复。

### Q: Claude Code 模式下任务没有执行？

1. 确认 `claude` 命令可用：`which claude`
2. 确认已认证：`claude auth`
3. 检查 `ANTHROPIC_API_KEY` 环境变量
4. 查看 `log/` 目录下的日志

### Q: Linear issue 没有被认领？

1. 确认 issue 状态在 `active_states` 列表中（如 Todo）
2. 确认 `project_slug` 配置正确（从项目 URL 获取）
3. 确认 `LINEAR_API_KEY` 有该项目的访问权限

### Q: 工作区 clone 失败？

1. 检查 `hooks.after_create` 中的 Git 仓库 URL 是否可访问
2. 确认 SSH Key 或认证信息已配置
3. 确认 `workspace.root` 目录有写入权限

### Q: 如何获取 Linear 项目 slug？

在 Linear 中右键点击项目 → 复制项目链接，URL 中包含 slug。例如 `https://linear.app/team/project/slug-name` 中的 `slug-name`。
